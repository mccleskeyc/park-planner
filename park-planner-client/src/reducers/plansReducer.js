const initialState = {
    plans: [],
    loading: true
}
const plansReducer = (state, action) => {
    switch(action.type) {
        default:
            return state;
    }
}

export default plansReducer