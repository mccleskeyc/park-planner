const initialState = {
    plans: [],
    loading: true
}
const plansReducer = (state=initialState, action) => {
    switch(action.type) {
        default:
            return state;
    }
}

export default plansReducer